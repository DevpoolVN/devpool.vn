# Google Apps Script Web App URL — nhận POST từ form Register.
# Tạo: scripts/google-apps-script/Code.gs (xem hướng dẫn trong file).
# Phải có prefix PUBLIC_ để Astro expose ra client.
PUBLIC_GAS_URL=https://script.google.com/macros/s/REPLACE_ME/exec
